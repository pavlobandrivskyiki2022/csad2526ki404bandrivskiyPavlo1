// Prompt: Create a minimal header with include guards (or #pragma once) that declares an int add(int a, int b) function in a simple namespace, suitable for unit testing.
#pragma once
namespace math_operations {
    int add(int a, int b);
}
