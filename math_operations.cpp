// Prompt: Implement the add(int a, int b) function declared in math_operations.h. Keep it trivial and include the header.
#include "math_operations.h"
namespace math_operations {
    int add(int a, int b) {
        return a + b;
    }
}